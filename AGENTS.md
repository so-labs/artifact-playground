# AI/開発者向け指示書

このファイルには、本プロジェクト（Artifact Playground）に機能やツールを追加する際、AIエージェントまたは人間の開発者が厳格に遵守すべきUI、アーキテクチャ、テストの実装ルールを定義しています。

---

## 🎨 1. UI・スタイルルール

- **カラーシステムとブランドカラー**:
  - **メインのブランドカラーは「青」**、**サブのブランドカラーは「オレンジ」**（青の補色）です。
  - アプリ内の「プライマリーカラー切替」機能で青とオレンジが交互に切り替わるのは、このブランドコンセプトに基づいています。
  - ライトモードとダークモードで視認性を保つため、青・オレンジともに微調整された異なる色合いが定義されています。
  - そのため、ボタン、ラジオボタン、トグルスイッチ、アクティブな枠線などには色コードを直書きせず、必ずCSS変数を利用してください。
    - 通常時: `--primary-color`
    - マウスホバー時: `--primary-hover`
- **UIコンポーネント制限**:
  - **チェックボックス（`<input type="checkbox">`）は絶対に使用しないでください。**
  - オン/オフの切り替えが必要な場合は、代わりに `components.css` に定義されている「**トグルスイッチ（Toggle Switch）**」を使用してください。

---

## 🏗 2. アーキテクチャと共通モジュールルール

- **共通ライブラリの利用**:
  - 複雑な処理、DOM操作、複数のツールで再利用するロジック（例: Markdownパーサー、Storage操作など）は、個別のツール内には記述せず、必ず `js/lib/` 配下に共通ライブラリ（モジュール）化して切り出してください。
- **多言語対応（i18n）の必須化**:
  - 新しいツールや機能を追加する際は、UIに表示されるすべてのテキスト（タイトル、説明文、ボタンラベル、プレースホルダーなど）を `js/lib/i18n.js` の `translations` オブジェクトに追加してください。
  - 対応言語（`ja`, `en` など）すべてに翻訳キーを追加してください。一部の言語だけに追加して他を省略することは禁止です。
  - HTML内にUIテキストを直書きせず、必ず i18n のキーを `data-i18n` 属性などで参照する形にしてください。
  - **Single Source of Truth**:
  - テキストの正は `i18n.js` の `translations` オブジェクトです。HTML要素内の直書きテキスト（例: `<h2 data-i18n="key">タイトル</h2>` の「タイトル」部分）は、コードの可読性を保つためのフォールバックとして残しますが、テキストを修正する際は**必ず `i18n.js` 側を更新**してください。
  - HTML側も合わせて更新するのが望ましいですが、不整合が生じた場合は `i18n.js` が優先されます。
- **モジュール化された既存モジュール**:
  - ストレージやコピー処理: `js/lib/storage.js`
  - Markdownの見出し処理: `js/lib/markdown-headings.js`

---

## 🧪 3. テストランナーと動作確認のルール

ツールを追加・変更した際は、必ずテストランナー（`tools/test-runner/test-runner.js`）に連動させてください。

- **テストランナーへの追加**:
  - 新機能を追加したら、必ず動作テストを `tools/test-runner/test-runner.js` に追加してください。
- **記述言語**:
  - テストの `describe` や `it` の説明文は、**原則として日本語で記述**してください。
  - ツール名やファイルパスなど、固有名詞・技術用語はそのまま英語表記で構いません。
- **命名規則と構造**:
  - 同じ機能のテストは、一番外側に「**機能名**」の `describe` を置き、その中に対象となる「**関数名 [対象ファイルパス]**」の `describe` をネスト（階層化）して記述してください。
  - **例**:

    ```javascript
    describe('20% Off', () => {
        describe('reduceText [tools/20-off/20-off.js]', () => {
            it('空テキストは空文字を返すこと', () => { ... });
        });
    });
    ```

- **サンプルの提供**:
  - 追加した機能の入力エリアには、ユーザーが初期状態で動作を試せるように、必ず **デフォルトのサンプルテキスト** を設定してください。
  - **サンプルテキストのトーン＆マナー**:
    - 日本の童謡（桃太郎、浦島太郎など）や、少しユーモア・クスッと笑えるような、「**真面目すぎない・遊び心のある内容**」を選定してください。
    - お堅いビジネス文書や無機質なプレースホルダー（「ここにテキストを入力」など）は避け、ユーザーが触って楽しいと感じる題材にしてください。

---

## 📦 4. リリース・バージョン管理ルール

- **キャッシュのバージョン管理 (`sw.js`)**:
  - Service Workerで使用するキャッシュ名 (`CACHE_NAME`) は、以下の規則で運用してください。
  - フォーマット: `artifact-playground-YYYY.MM-rN`
    - **YYYY**: 西暦 (例: 2026)
    - **MM**: 月 (01〜12)
    - **rN**: その月のリリース回数 (例: r1, r2...)
  - 例: `artifact-playground-2026.07-r1`
  - ファイル追加やロジック変更などを行った際は、必ず `sw.js` 内のこのバージョン文字列を更新し、ユーザーのブラウザにある古いキャッシュが安全に破棄・更新されるようにしてください。
  - **Single Source of Truth**: アプリ内のバージョン表記は、この `sw.js` の `CACHE_NAME` から動的に抽出して表示する仕組みになっています。バージョンを一箇所に集約するため、他のファイルにバージョン文字列を直書きしないでください。
